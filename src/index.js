import React from 'react';
import ReactDOM from 'react-dom';
import './styles/Index.css'; // Make sure Index.css exists
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
